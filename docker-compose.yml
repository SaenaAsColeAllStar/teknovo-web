# Infra is external on this VPS — do NOT run local Postgres / MinIO / Redis here.
# Use aaPanel PostgreSQL + Redis and the existing MinIO container (minio_webhost).
# See .env.local (append-only notes) and apps/api/.env.example for connection shapes.
#
# Production Node API secrets: set in aaPanel environment variables (not a file).
# Optional: keep this file empty of data services so `pnpm docker:up` is a no-op
# until/unless you add app-only sidecars later.

services: {}
