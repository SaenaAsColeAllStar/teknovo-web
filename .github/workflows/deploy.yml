# DISABLED — OpenNext monolit tidak dipakai di Free.
# Gunakan: deploy-api.yml, deploy-cms.yml, rebuild-web.yml
# Lihat DEPLOY.md

name: Legacy OpenNext (disabled)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type DEPLOY_LEGACY_OPENNEXT to run (Workers Paid only)"
        required: true

jobs:
  skip:
    if: github.event.inputs.confirm == 'DEPLOY_LEGACY_OPENNEXT'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy OpenNext path — requires Workers Paid. Prefer Split Free workflows."
