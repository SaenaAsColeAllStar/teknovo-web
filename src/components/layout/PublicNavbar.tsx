/**
 * Navbar situs publik — re-export three-tier marketing chrome.
 * Dipakai `PublicSiteLayout` (landing & admissions).
 */
export { PublicMarketingNavbar as PublicNavbar } from "@/components/layout/PublicMarketingNavbar";
